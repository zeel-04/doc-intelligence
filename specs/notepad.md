[x] The client API design issue.(the processor creates a 1:1 mapping with doc so for other we have to create a new one, also there maybe other improvements as well)
[] The validation logic of the config provided from user can be streamlined and improved
